# LEV — Auros-inspired portfolio

Standalone static portfolio created from `DESIGN (3).md`.

Run with any static server, for example:

```powershell
python -m http.server 5190
```

Then open `http://127.0.0.1:5190/`.
